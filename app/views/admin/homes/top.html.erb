<div class="container">
 <div class="row">
  <div class="col-sm-12 col-md-11 col-lg-8 px-5 px-sm-0 mx-auto">
   <br>
　 <h3>注文履歴一覧</h3>
   <table class='table table-hover table-inverse'>
    <thead>
      <tr>
        <th>購入日時</th>
        <th>購入者</th>
        <th>注文個数</th>
        <th>注文ステータス</th>
      </tr>
    <tbody>
      <% @order_products.each do |order_product| %>
      <td><%= order_product.created_at %></td>
      <td><%= "order_product.user.last_name" %> <%= "order_product.user.first_name" %></td>
      <td><%= order_product.count %></td>
      <td><%= order.status %></td>
      <% end %>
    </tbody>
    </thead>
   </table>

  </div>
 </div>
</div>